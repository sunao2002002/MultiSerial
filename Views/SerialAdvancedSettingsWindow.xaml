<Window x:Class="SerialApp.Desktop.Views.SerialAdvancedSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="高级串口配置"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="数据位"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding DataBitsOptions}" SelectedItem="{Binding SelectedDataBits}"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="起始位"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding StartBitsOptions}" SelectedItem="{Binding SelectedStartBits}"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="停止位"/>
            <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" ItemsSource="{Binding StopBitsOptions}" SelectedItem="{Binding SelectedStopBits}"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="校验位"/>
            <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding ParityOptions}" SelectedItem="{Binding SelectedParity}"/>
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,18,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="96" Margin="0,0,8,0" Content="取消" Click="CancelButton_Click"/>
            <Button Width="96" Content="确定" Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>