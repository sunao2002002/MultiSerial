<UserControl x:Class="SerialApp.Desktop.Views.SplitPanelNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid x:Name="LayoutGrid">
        <ContentControl x:Name="FirstContentHost"
                        Content="{Binding First}"
                        Margin="0,0,6,0"/>
        <GridSplitter x:Name="PanelSplitter"
                      Background="{StaticResource SurfaceMutedBrush}"
                      ShowsPreview="True"/>
        <ContentControl x:Name="SecondContentHost"
                        Content="{Binding Second}"
                        Margin="6,0,0,0"/>
    </Grid>
</UserControl>